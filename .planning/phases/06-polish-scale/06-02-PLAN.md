---
wave: 1
depends_on:
  - 06-01-PLAN.md
files_modified:
  - src/components/auth/role-badge.tsx
  - src/app/profile/page.tsx
  - src/components/library/book-card.tsx
  - src/components/library/bookshelf.tsx
  - src/components/library/empty-library.tsx
  - src/app/(library)/layout.tsx
  - src/components/auth/__tests__/role-badge.test.tsx
autonomous: true
requirements:
  - POL-02
  - POL-05
---

# Plan 06-02: Progress Bar, Pro Badges, and Bookshelf Polish

## Objective
Deliver the visual layer for Phase 6: progress bars on book cards, Pro/Admin badges in the library header, and bookshelf hover/typography polish.

## Tasks

### Task 06-02-01: Extract RoleBadge to shared component

<read_first>
- src/app/profile/page.tsx (reason: extract existing RoleBadge)
- src/components/ui/badge.tsx (reason: confirm Badge API)
- src/types/book.ts (reason: confirm UserRole type export)
</read_first>

<action>
1. Create `src/components/auth/role-badge.tsx` with this exact content:

```tsx
import { Badge } from "@/components/ui/badge";
import { Shield } from "lucide-react";
import type { UserRole } from "@/types/book";

interface RoleBadgeProps {
  role: UserRole;
}

export function RoleBadge({ role }: RoleBadgeProps) {
  switch (role) {
    case "admin":
      return (
        <Badge variant="outline" className="gap-1">
          <Shield className="h-3 w-3" />
          Admin
        </Badge>
      );
    case "pro":
      return <Badge className="bg-slate-900 text-white">Pro</Badge>;
    case "regular":
    default:
      return null;
  }
}
```

2. Update `src/app/profile/page.tsx` to import and use the shared `RoleBadge`, while keeping the "Regular" display for regular users in the profile context. Replace the existing inline `RoleBadge` function with:

```tsx
import { RoleBadge } from "@/components/auth/role-badge";
```

And update the badge display section to:
```tsx
<div className="mt-2 flex items-center gap-3">
  {user.role === "regular" ? (
    <Badge variant="secondary">Regular</Badge>
  ) : (
    <RoleBadge role={user.role} />
  )}
  {user.role === "regular" && (
    <span className="cursor-not-allowed text-sm text-muted-foreground">
      Upgrade to Pro
    </span>
  )}
</div>
```

Remove the old inline `function RoleBadge` definition from the file. Keep all other profile page content unchanged.
</action>

<acceptance_criteria>
- `src/components/auth/role-badge.tsx` exists and exports `RoleBadge`
- `grep "case \"regular\"" src/components/auth/role-badge.tsx` returns a match with `return null;`
- `grep "function RoleBadge" src/app/profile/page.tsx` returns NO match (old inline function removed)
- `grep "@/components/auth/role-badge" src/app/profile/page.tsx` returns a match
</acceptance_criteria>

### Task 06-02-02: Update BookCard with progress bar, hover effects, and typography

<read_first>
- src/components/library/book-card.tsx (reason: full rewrite of cover + text area)
- src/components/reader/reading-progress.tsx (reason: copy progress bar pattern)
- src/components/library/bookshelf.tsx (reason: confirm Book interface)
</read_first>

<action>
Rewrite `src/components/library/book-card.tsx` to this exact content:

```tsx
import Link from "next/link";
import { BookOpen } from "lucide-react";

interface BookCardProps {
  id: string;
  title: string;
  author: string | null;
  language: string;
  coverPath: string | null;
  progress?: number | null;
}

const PLACEHOLDER_COLORS = [
  "#cbd5e1", "#94a3b8", "#64748b", "#475569", "#334155", "#1e293b",
];

function getPlaceholderColor(title: string): string {
  let hash = 0;
  for (let i = 0; i < title.length; i++) {
    hash = (hash * 31 + title.charCodeAt(i)) | 0;
  }
  return PLACEHOLDER_COLORS[Math.abs(hash) % PLACEHOLDER_COLORS.length];
}

export function BookCard({ id, title, author, language, coverPath, progress }: BookCardProps) {
  const bgColor = getPlaceholderColor(title);

  return (
    <Link href={`/book/${id}`} className="group block">
      <div className="overflow-hidden rounded-md transition-shadow duration-200 hover:shadow-md">
        <div className="relative aspect-[3/4] w-full bg-slate-100">
          {coverPath ? (
            <img
              src={`/api/files/covers/${id}.jpg`}
              alt={title}
              className="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105"
            />
          ) : (
            <div
              className="flex h-full w-full items-center justify-center bg-gradient-to-br from-white/10 to-black/10"
              style={{ backgroundColor: bgColor }}
            >
              <BookOpen className="h-12 w-12 text-white/40" />
            </div>
          )}
          {language && language !== "und" && (
            <span className="absolute right-1.5 top-1.5 rounded bg-white/90 px-1.5 py-0.5 text-xs font-medium text-slate-700 shadow-sm">
              {language.toUpperCase()}
            </span>
          )}
          {progress !== undefined && progress !== null && progress > 0 && (
            <>
              <div
                className="absolute bottom-0 left-0 right-0 h-1 bg-black/10"
                role="progressbar"
                aria-valuenow={Math.round(progress)}
                aria-valuemin={0}
                aria-valuemax={100}
                aria-label={`Reading progress: ${Math.round(progress)}%`}
              >
                <div
                  className="h-full bg-primary transition-all duration-300"
                  style={{ width: `${progress}%` }}
                />
              </div>
              <span className="absolute bottom-1.5 right-1.5 text-[10px] font-medium text-white drop-shadow-sm">
                {Math.round(progress)}%
              </span>
            </>
          )}
        </div>
        <div className="px-1 pt-2">
          <h3 className="line-clamp-2 text-base font-semibold leading-tight text-slate-900">
            {title}
          </h3>
          {author && (
            <p className="mt-1 truncate text-sm text-muted-foreground">
              {author}
            </p>
          )}
        </div>
      </div>
    </Link>
  );
}
```

Key changes enforced by the rewrite:
- `progress?: number | null` added to `BookCardProps`
- Outer `div` gains `transition-shadow duration-200 hover:shadow-md`
- Cover `img` gains `transition-transform duration-300 group-hover:scale-105`
- Placeholder `div` gains `bg-gradient-to-br from-white/10 to-black/10`
- Progress bar renders only when `progress > 0` with `role="progressbar"` + aria attributes
- Percentage label at `bottom-1.5 right-1.5` with `text-[10px] font-medium text-white drop-shadow-sm`
- Title changed from `text-[20px]` to `text-base`
- Text container gains `px-1`
</action>

<acceptance_criteria>
- `src/components/library/book-card.tsx` contains `progress?: number | null` in `BookCardProps`
- `grep "hover:shadow-md" src/components/library/book-card.tsx` succeeds
- `grep "group-hover:scale-105" src/components/library/book-card.tsx` succeeds
- `grep "text-base font-semibold" src/components/library/book-card.tsx` succeeds (title size)
- `grep "role=\"progressbar\"" src/components/library/book-card.tsx` succeeds
- `grep "drop-shadow-sm" src/components/library/book-card.tsx` succeeds
- `grep "bg-gradient-to-br" src/components/library/book-card.tsx` succeeds
- `grep "px-1 pt-2" src/components/library/book-card.tsx` succeeds
</acceptance_criteria>

### Task 06-02-03: Update Bookshelf grid, skeleton, and EmptyLibrary

<read_first>
- src/components/library/bookshelf.tsx (reason: update grid gaps and pass progress)
- src/components/library/empty-library.tsx (reason: icon size + min-height polish)
</read_first>

<action>
1. Rewrite `src/components/library/bookshelf.tsx` to this exact content:

```tsx
import { BookCard } from "./book-card";
import { Skeleton } from "@/components/ui/skeleton";

interface Book {
  id: string;
  title: string;
  author: string | null;
  language: string;
  coverPath: string | null;
  progress?: number | null;
}

interface BookshelfProps {
  books: Book[];
}

export function Bookshelf({ books }: BookshelfProps) {
  return (
    <div className="grid grid-cols-[repeat(auto-fill,minmax(200px,1fr))] gap-x-6 gap-y-8">
      {books.map((book) => (
        <BookCard
          key={book.id}
          id={book.id}
          title={book.title}
          author={book.author}
          language={book.language}
          coverPath={book.coverPath}
          progress={book.progress}
        />
      ))}
    </div>
  );
}

export function BookshelfSkeleton() {
  return (
    <div className="grid grid-cols-[repeat(auto-fill,minmax(200px,1fr))] gap-x-6 gap-y-8">
      {Array.from({ length: 8 }).map((_, i) => (
        <div key={i}>
          <Skeleton className="aspect-[3/4] w-full rounded-md" />
          <div className="space-y-2 px-1 pt-2">
            <Skeleton className="h-5 w-[70%]" />
            <Skeleton className="h-4 w-[40%]" />
          </div>
        </div>
      ))}
    </div>
  );
}
```

2. Update `src/components/library/empty-library.tsx` to this exact content:

```tsx
import { BookOpen } from "lucide-react";
import { UploadDropzone } from "./upload-dropzone";

export function EmptyLibrary() {
  return (
    <div className="flex min-h-[50vh] flex-col items-center justify-center">
      <BookOpen className="h-16 w-16 text-muted-foreground" />
      <h2 className="mt-4 text-[28px] font-semibold text-slate-900">
        Your library is empty
      </h2>
      <p className="mt-2 max-w-[400px] text-center text-base text-muted-foreground">
        Upload your first EPUB to start reading with AI-powered explanations.
      </p>
      <div className="mt-6 w-full max-w-md">
        <UploadDropzone />
      </div>
    </div>
  );
}
```
</action>

<acceptance_criteria>
- `src/components/library/bookshelf.tsx` contains `gap-x-6 gap-y-8`
- `src/components/library/bookshelf.tsx` contains `progress={book.progress}`
- `src/components/library/bookshelf.tsx` skeleton contains `gap-x-6 gap-y-8`
- `src/components/library/bookshelf.tsx` skeleton contains `px-1 pt-2`
- `src/components/library/empty-library.tsx` contains `min-h-[50vh]`
- `src/components/library/empty-library.tsx` contains `h-16 w-16`
</acceptance_criteria>

### Task 06-02-04: Convert LibraryLayout to async server component with RoleBadge

<read_first>
- src/app/(library)/layout.tsx (reason: convert to async, add requireAuth + RoleBadge)
- src/lib/auth-guards.ts (reason: confirm requireAuth return type)
- src/components/auth/role-badge.tsx (reason: import new component)
</read_first>

<action>
Rewrite `src/app/(library)/layout.tsx` to this exact content:

```tsx
import { requireAuth } from "@/lib/auth-guards";
import { RoleBadge } from "@/components/auth/role-badge";
import { UserNav } from "@/components/auth/user-nav";
import Link from "next/link";

export default async function LibraryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const user = await requireAuth();

  return (
    <div className="min-h-screen bg-slate-50">
      <header className="flex h-16 items-center border-b border-slate-200 bg-white px-8">
        <Link href="/my-library" className="text-[20px] font-semibold text-slate-900">
          BusyReader
        </Link>
        <nav className="mx-auto flex items-center gap-6">
          <Link
            href="/my-library"
            className="text-sm font-medium text-slate-600 hover:text-slate-900"
          >
            My Library
          </Link>
        </nav>
        <div className="flex items-center gap-3">
          <RoleBadge role={user.role} />
          <UserNav />
        </div>
      </header>
      <main className="mx-auto max-w-[1280px] px-8 py-6">{children}</main>
    </div>
  );
}
```

Key changes:
- Function signature changed from `export default function LibraryLayout` to `export default async function LibraryLayout`
- `const user = await requireAuth();` added at top of function body
- `<RoleBadge role={user.role} />` inserted before `<UserNav />` inside a `flex items-center gap-3` wrapper
</action>

<acceptance_criteria>
- `src/app/(library)/layout.tsx` contains `export default async function LibraryLayout`
- `src/app/(library)/layout.tsx` contains `const user = await requireAuth();`
- `src/app/(library)/layout.tsx` contains `<RoleBadge role={user.role} />`
- `src/app/(library)/layout.tsx` contains `flex items-center gap-3`
- `grep "async function LibraryLayout" src/app/(library)/layout.tsx` succeeds
</acceptance_criteria>

### Task 06-02-05: Unit test for RoleBadge component rendering

<read_first>
- src/components/auth/role-badge.tsx (reason: test all three role branches)
- vitest.config.ts (reason: confirm test environment and file patterns)
</read_first>

<action>
Create `src/components/auth/__tests__/role-badge.test.tsx` with this exact content:

```tsx
import { describe, it, expect } from "vitest";
import { renderToStaticMarkup } from "react-dom/server";
import { RoleBadge } from "../role-badge";

function render(el: React.ReactElement) {
  return renderToStaticMarkup(el);
}

describe("POL-05: RoleBadge", () => {
  it("renders Pro badge for pro role", () => {
    const html = render(<RoleBadge role="pro" />);
    expect(html).toContain("Pro");
    expect(html).toContain("bg-slate-900");
  });

  it("renders Admin badge with Shield icon for admin role", () => {
    const html = render(<RoleBadge role="admin" />);
    expect(html).toContain("Admin");
    expect(html).toContain("Shield"); // lucide-react renders SVG with this identifier
  });

  it("returns nothing for regular role", () => {
    const html = render(<RoleBadge role="regular" />);
    expect(html).toBe("");
  });
});
```
</action>

<acceptance_criteria>
- `src/components/auth/__tests__/role-badge.test.tsx` exists
- `pnpm test role-badge.test.tsx` exits 0 with all 3 tests passing
- Tests verify: "pro" renders "Pro", "admin" renders "Admin", "regular" renders empty string
</acceptance_criteria>

## Verification

<verify>
- [ ] `grep "RoleBadge" src/app/(library)/layout.tsx` succeeds
- [ ] `grep "progress" src/components/library/book-card.tsx` succeeds
- [ ] `grep "gap-x-6 gap-y-8" src/components/library/bookshelf.tsx` succeeds
- [ ] `grep "min-h-\[50vh\]" src/components/library/empty-library.tsx` succeeds
- [ ] `grep "export function RoleBadge" src/components/auth/role-badge.tsx` succeeds
- [ ] `pnpm test role-badge.test.tsx` passes all 3 tests
- [ ] `pnpm build` passes with no TypeScript errors
- [ ] `pnpm test` passes (existing + new tests)
</verify>

## must_haves

- `RoleBadge` is extracted to `src/components/auth/role-badge.tsx` and returns `null` for regular users
- `BookCard` renders a thin `h-1` progress bar at the bottom of the cover when `progress > 0`, with aria attributes and percentage overlay
- `BookCard` cover image scales to `105%` on hover with `duration-300`, and card container gains `shadow-md` on hover with `duration-200`
- `BookCard` title is `text-base` (down from `text-[20px]`), text area has `px-1`
- `Bookshelf` grid uses `gap-x-6 gap-y-8` and passes `progress` to each `BookCard`
- `EmptyLibrary` icon is `h-16 w-16` with `min-h-[50vh]`
- `LibraryLayout` is an `async` server component calling `requireAuth()` and renders `<RoleBadge role={user.role} />` in the header between nav and `UserNav`
- All 3 RoleBadge unit tests pass
