---
phase: 02-core-reading
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reader/toc-panel.tsx
  - src/components/reader/theme-toggle.tsx
  - src/components/reader/reader-skeleton.tsx
  - src/components/reader/reader-error.tsx
  - src/components/reader/reader-client.tsx
autonomous: true
requirements:
  - READ-01
  - READ-02
  - READ-03
  - READ-04
must_haves:
  truths:
    - ToC panel is a left Sheet w-[320px] sm:w-[360px] with hierarchical entries
    - Clicking a ToC entry calls onNavigate(href) and closes the Sheet
    - Theme toggle cycles light → sepia → dark with mount-gating
    - Theme sync propagates to rendition.themes.select() inside the iframe
    - Skeleton and error states match UI-SPEC dimensions and copy exactly
  artifacts:
    - src/components/reader/toc-panel.tsx
    - src/components/reader/theme-toggle.tsx
    - src/components/reader/reader-skeleton.tsx
    - src/components/reader/reader-error.tsx
  key_links:
    - TocPanel must use shadcn Sheet side="left" with ScrollArea for list
    - ThemeToggle must not render until mounted (hydration safety)
    - Reader client must replace stubs with real TocPanel and ThemeToggle
---

<objective>
Build the Table of Contents panel, theme toggle, and loading/error states. Replace the stubs in reader-client with real components. This delivers ToC navigation and three-theme support.

Purpose: ToC navigation and theme switching are core user-facing features. Without them, the reader is incomplete.

Output: ToC panel, theme toggle, skeleton, error states, and updated reader-client composition.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-reading/02-CONTEXT.md
@.planning/phases/02-core-reading/02-RESEARCH.md
@.planning/phases/02-core-reading/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ToC panel with shadcn Sheet and hierarchical entries</name>
  <files>src/components/reader/toc-panel.tsx</files>
  <action>
    Create src/components/reader/toc-panel.tsx:
    - Props: toc: NavItem[], currentHref?: string, onNavigate: (href: string) => void
    - Use shadcn Sheet, SheetTrigger, SheetContent, SheetHeader, SheetTitle
    - SheetTrigger: Button variant="ghost" size="icon-sm" with Menu icon, aria-label="Open table of contents"
    - SheetContent: side="left", className="w-[320px] sm:w-[360px] p-0"
    - SheetHeader: px-4 py-3 border-b border-border, SheetTitle="Table of Contents"
    - Inside ScrollArea (className="h-[calc(100vh-56px)]"): render recursive TocEntry component
    - TocEntry: receives item (NavItem), onNavigate, level (default 0)
    - Render button with className="w-full text-left py-2 pr-4 text-sm transition-colors"
    - Active entry: "font-medium text-primary"; inactive: "text-foreground hover:text-primary"
    - Indent: style={{ paddingLeft: level > 0 ? `${level * 16 + 16}px` : '16px' }}
    - For level > 0, add "border-l-2 border-border ml-4" for visual hierarchy
    - On click: call onNavigate(item.href) — the parent closes the Sheet via Sheet onOpenChange
    - Recursively render item.subitems?.map(child => <TocEntry ... level={level + 1} />)
    - Export both TocPanel (with Sheet wrapper) and TocEntry
  </action>
  <verify>
    <automated>test -f src/components/reader/toc-panel.tsx && grep -q "side=\"left\"" src/components/reader/toc-panel.tsx && grep -q "w-\[320px\]" src/components/reader/toc-panel.tsx && grep -q "ScrollArea" src/components/reader/toc-panel.tsx</automated>
  </verify>
  <done>ToC panel is a left Sheet with ScrollArea, recursive entries, 16px indent steps, and active state styling.</done>
</task>

<task type="auto">
  <name>Task 2: Build theme toggle with mount-gating and cycle logic</name>
  <files>src/components/reader/theme-toggle.tsx</files>
  <action>
    Create src/components/reader/theme-toggle.tsx:
    - 'use client'
    - Import useTheme from 'next-themes'
    - Import { Sun, Moon, BookOpen } from 'lucide-react'
    - Import { Button } from '@/components/ui/button'
    - Component ThemeToggle: no props
    - State: const [mounted, setMounted] = useState(false)
    - useEffect(() => setMounted(true), [])
    - const { resolvedTheme, setTheme } = useTheme()
    - cycleTheme(): if resolvedTheme === 'light' → setTheme('sepia'); if 'sepia' → setTheme('dark'); else → setTheme('light')
    - If !mounted: return <div className="h-7 w-7" /> (placeholder matching icon-sm button dimensions)
    - Render: <Button variant="ghost" size="icon-sm" onClick={cycleTheme} aria-label="Cycle theme (light, sepia, dark)">
        {resolvedTheme === 'light' && <Sun className="h-4 w-4" />}
        {resolvedTheme === 'sepia' && <BookOpen className="h-4 w-4" />}
        {resolvedTheme === 'dark' && <Moon className="h-4 w-4" />}
      </Button>
    - Note: The parent (reader-client or epub-viewer) handles calling rendition.themes.select(theme) when resolvedTheme changes
  </action>
  <verify>
    <automated>test -f src/components/reader/theme-toggle.tsx && grep -q "useTheme" src/components/reader/theme-toggle.tsx && grep -q "setTheme('sepia')" src/components/reader/theme-toggle.tsx && grep -q "h-7 w-7" src/components/reader/theme-toggle.tsx</automated>
  </verify>
  <done>Theme toggle cycles light→sepia→dark, is mount-gated, and renders correct icons per theme.</done>
</task>

<task type="auto">
  <name>Task 3: Build skeleton, error states, and wire into reader-client</name>
  <files>src/components/reader/reader-skeleton.tsx, src/components/reader/reader-error.tsx, src/components/reader/reader-client.tsx</files>
  <action>
    Create src/components/reader/reader-skeleton.tsx:
    - Absolute inset-0 z-60 flex flex-col items-center justify-center bg-background
    - Content: Skeleton h-8 w-48 (title), Skeleton h-4 w-32 (subtitle), then mt-8 space-y-3 w-[300px] with 5 Skeleton lines of varying widths (92%, 96%, 88%, 94%, full)

    Create src/components/reader/reader-error.tsx:
    - Props: onBack: () => void, onRetry: () => void
    - Absolute inset-0 z-60 flex items-center justify-center bg-background
    - Card with mx-4 max-w-sm, CardContent flex flex-col items-center gap-4 pt-6
    - AlertCircle icon h-12 w-12 text-destructive
    - h2: "Could not load book" (text-base font-medium)
    - p: "There was a problem opening this book. Try again or return to your library." (text-center text-sm text-muted-foreground)
    - Two buttons: "Back to Library" (variant=outline, onClick=onBack) and "Try Again" (onClick=onRetry)

    Update src/components/reader/reader-client.tsx:
    - Replace TocTrigger stub with real TocPanel, passing toc state, currentHref (derive from rendition if possible), and onNavigate handler
    - Replace ThemeToggleStub with real ThemeToggle
    - Add isLoaded state from EpubViewer; show ReaderSkeleton when !isLoaded && !error
    - Add error boundary or try/catch around EpubViewer mount; show ReaderError on error
    - onNavigate handler: call viewerRef.current?.navigateTo(href)
    - Import ReaderSkeleton and ReaderError
  </action>
  <verify>
    <automated>test -f src/components/reader/reader-skeleton.tsx && test -f src/components/reader/reader-error.tsx && grep -q "ReaderSkeleton" src/components/reader/reader-client.tsx && grep -q "TocPanel" src/components/reader/reader-client.tsx && grep -q "ThemeToggle" src/components/reader/reader-client.tsx</automated>
  </verify>
  <done>Skeleton and error states exist with exact UI-SPEC copy. Reader client uses real TocPanel and ThemeToggle. Reader skeleton shows during load, error card shows on failure.</done>
</task>

</tasks>

<verification>
- Playwright test: open reader → click ToC trigger → verify Sheet opens with side="left"
- Playwright test: click ToC entry → verify rendition.display(href) called (spy on ref method)
- Unit test: ThemeToggle renders placeholder before mount, cycles themes correctly after mount
- Manual: verify skeleton shows 5 lines with varying widths, error card has exact copy from UI-SPEC
- Manual: theme toggle cycles light→sepia→dark, icons change, no hydration errors
</verification>

<success_criteria>
- ToC panel opens as left Sheet with hierarchical chapter/section list
- Clicking ToC entry navigates to section and closes Sheet
- Theme toggle cycles light → sepia → dark with correct icons
- No hydration mismatch (mount-gating prevents undefined theme on server)
- Skeleton displays during EPUB loading with 5 lines of varying widths
- Error state displays exact copy: "Could not load book" / "There was a problem opening this book..."
- Reader client integrates all components without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-reading/02-03-SUMMARY.md`
</output>
