---
phase: 02-core-reading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/db/schema.prisma
  - src/server/db/index.ts
  - src/components/providers.tsx
  - src/app/globals.css
  - src/app/(reader)/layout.tsx
  - src/app/(reader)/book/[id]/reader/page.tsx
  - src/app/(library)/book/[id]/page.tsx
autonomous: true
requirements:
  - READ-01
  - READ-02
must_haves:
  truths:
    - Database has UserBookPosition table with @@unique([userId, bookId])
    - /book/[id]/reader route exists inside (reader) group and is auth-gated
    - ThemeProvider wraps app with themes=['light','dark','sepia'], enableSystem=false
    - Sepia CSS variables are defined in globals.css
    - "Open Reader" button on book detail page navigates to /book/[id]/reader
  artifacts:
    - src/server/db/schema.prisma (updated with UserBookPosition)
    - prisma/migrations/ (new migration applied)
    - src/app/(reader)/layout.tsx
    - src/app/(reader)/book/[id]/reader/page.tsx
    - src/components/providers.tsx (with ThemeProvider)
    - src/app/globals.css (with .sepia block)
    - src/app/(library)/book/[id]/page.tsx (enabled Open Reader button)
  key_links:
    - ThemeProvider must use attribute="class" and disable system preference
    - Reader layout must be h-screen w-screen overflow-hidden with no header
    - Reader page must call requireAuth() and getBookForUser() before rendering
---

<objective>
Establish the reader infrastructure: database schema for position persistence, full-screen route group, theme provider with sepia support, and navigation from book detail to reader.

Purpose: Without this foundation, no reader components can mount, no themes can apply, and no positions can be saved.

Output: Database migrated, route group created, theme system wired, entry point enabled.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-reading/02-CONTEXT.md
@.planning/phases/02-core-reading/02-RESEARCH.md
@.planning/phases/02-core-reading/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UserBookPosition model and migrate</name>
  <files>src/server/db/schema.prisma</files>
  <action>
    Add the following model to schema.prisma:
    
    model UserBookPosition {
      id              String   @id @default(cuid())
      userId          String
      bookId          String
      paragraphIndex  Int
      charOffset      Int
      cfi             String?  // runtime fallback for instant resume
      tocSectionId    String?  // optional: which TOC section for fast nav
      updatedAt       DateTime @updatedAt

      user User     @relation(fields: [userId], references: [id], onDelete: Cascade)
      book EpubFile @relation(fields: [bookId], references: [id], onDelete: Cascade)

      @@unique([userId, bookId])
      @@index([userId])
      @@index([bookId])
    }
    
    Also add `positions UserBookPosition[]` to both User and EpubFile models.
    Then run: `npx prisma migrate dev --name add_user_book_position` and `npx prisma generate`.
  </action>
  <verify>
    <automated>npx prisma validate && grep -q "UserBookPosition" src/server/db/schema.prisma</automated>
  </verify>
  <done>Prisma schema validates, migration file exists in prisma/migrations/, and TypeScript types are generated.</done>
</task>

<task type="auto">
  <name>Task 2: Create (reader) route group with full-screen layout and reader page</name>
  <files>src/app/(reader)/layout.tsx, src/app/(reader)/book/[id]/reader/page.tsx</files>
  <action>
    Create src/app/(reader)/layout.tsx:
    - Export default function ReaderLayout({ children }) returning `<div className="h-screen w-screen overflow-hidden bg-background">{children}</div>`
    - No headers, no footers, no navigation chrome
    
    Create src/app/(reader)/book/[id]/reader/page.tsx:
    - Server component, async function ReaderPage({ params }: { params: Promise<{ id: string }> })
    - Call `const session = await requireAuth()` and `const { id } = await params`
    - Call `const book = await getBookForUser(id, session.user.id)` — if null, redirect to /my-library
    - Construct EPUB URL: `/api/files/${book.epubPath}` (or use storage abstraction)
    - Render `<ReaderClient book={book} epubUrl={epubUrl} userId={session.user.id} />`
    - Import ReaderClient from `@/components/reader/reader-client` (stub for now: `'use client'` component that returns `<div>Reader</div>`)
  </action>
  <verify>
    <automated>test -f src/app/(reader)/layout.tsx && test -f src/app/(reader)/book/[id]/reader/page.tsx && grep -q "h-screen w-screen overflow-hidden" src/app/(reader)/layout.tsx</automated>
  </verify>
  <done>Route files exist, layout is full-screen, reader page auth-gates and fetches book.</done>
</task>

<task type="auto">
  <name>Task 3: Wire ThemeProvider with sepia and enable Open Reader navigation</name>
  <files>src/components/providers.tsx, src/app/globals.css, src/app/(library)/book/[id]/page.tsx</files>
  <action>
    Update src/components/providers.tsx:
    - Wrap children with `<ThemeProvider attribute="class" defaultTheme="light" themes={['light', 'dark', 'sepia']} enableSystem={false} disableTransitionOnChange={false}>`
    - Import ThemeProvider from 'next-themes'
    
    Update src/app/globals.css:
    - Add the exact .sepia CSS variable block from 02-UI-SPEC.md (all 20+ variables including --background:#f4ecd8, --foreground:#5b4636, etc.)
    
    Update src/app/(library)/book/[id]/page.tsx:
    - Find the disabled "Open Reader" button and enable it
    - Change to: `<Button asChild><Link href={\`/book/\${book.id}/reader\`}>Open Reader</Link></Button>`
    - Or if no Link wrapper, use `onClick={() => router.push(\`/book/\${book.id}/reader\`)}`
    - Remove any `disabled` prop or explanatory tooltip about reader not being ready
  </action>
  <verify>
    <automated>grep -q "themes={\['light', 'dark', 'sepia'\]}" src/components/providers.tsx && grep -q "\.sepia {" src/app/globals.css && grep -q "/book/\${book.id}/reader" src/app/\(library\)/book/\[id\]/page.tsx</automated>
  </verify>
  <done>ThemeProvider configured with sepia, sepia CSS variables defined, Open Reader button navigates to reader route.</done>
</task>

</tasks>

<verification>
- `npx prisma validate` passes
- `npm run build` compiles without TypeScript errors in new files
- Visiting /book/[id]/reader while authenticated renders the stub reader page
- Visiting /book/[id]/reader while unauthenticated redirects to login
- Book detail page "Open Reader" button is enabled and links to reader route
- ThemeProvider sets `class="sepia"` on `<html>` when `setTheme('sepia')` is called
</verification>

<success_criteria>
- Database migration applied and UserBookPosition model queryable via Prisma
- (reader) route group renders full-screen without library header
- Reader page at /book/[id]/reader requires auth and resolves book access
- next-themes configured with light/dark/sepia, enableSystem=false
- Sepia CSS variables render correct parchment background (#f4ecd8) and brown text (#5b4636)
- Book detail page has working "Open Reader" navigation
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-reading/02-01-SUMMARY.md`
</output>
